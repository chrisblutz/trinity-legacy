module Trinity
    module StringUtils
        class Regex
            def initialize(matches, groups)
                @matches = matches
                @groups = groups

            def matches()
                @matches

            def groups
                @groups
