module Trinity
    module StringUtils
        class Regex
            def initialize(matches, groups)
                @matches = matches
                @groups = groups

            def matches()
                @matches

            def groups
                @groups

            def toString
                str = getClass().toString() + '[Matches: ' + matches() + ', ' + groups().length() + ' group(s)]'
